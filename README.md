# Lab 2 Fluid Simulation

Taichi implementation of a 3D FLIP/PIC particle fluid demo.

Run:

```bash
uv sync
uv run python main.py
```

The demo starts paused so the spherical obstacle can be placed before simulation.
