[project]
name = "fluid-sim"
version = "0.1.0"
description = "Taichi 2D PIC/FLIP fluid simulation for Lab 2"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "taichi>=1.6",
]

[tool.uv]
package = false
